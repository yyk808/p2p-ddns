# p2p-ddns 单机测试指南

本指南提供了如何在单台机器上利用Docker的隔离网络环境来测试p2p-ddns功能的完整说明。

## 概述

测试框架使用Docker创建多个隔离的虚拟网络，模拟真实的校园网/企业网环境，包括：

- **subnet-a** (10.0.1.0/24) - 模拟教学楼A网络
- **subnet-b** (10.0.2.0/24) - 模拟教学楼B网络
- **public** (10.0.0.0/24) - 模拟公共网络

## 前置要求

### 必需组件

1. **Docker** (版本 20.10+)
   ```bash
   docker --version
   ```

2. **Docker Compose** (版本 2.0+)
   ```bash
   docker-compose --version
   # 或
   docker compose version
   ```

3. **Rust** (用于构建项目)
   ```bash
   rustc --version
   cargo --version
   ```

4. **系统要求**
   - 至少 5GB 可用磁盘空间
   - 至少 2GB 可用内存
   - Docker服务正在运行

### 可选组件

- **jq** - 用于JSON输出处理
- **bc** - 用于数值计算（某些脚本使用）

## 快速开始

### 方式1：一键完整测试（推荐）

从项目根目录运行：

```bash
cd tests/integration
./single-machine-test.sh full
```

这将自动完成：
1. ✅ 检查Docker环境
2. ✅ 构建Docker镜像
3. ✅ 创建隔离网络
4. ✅ 启动所有测试容器
5. ✅ 运行完整测试套件
6. ✅ 生成测试报告

测试大约需要10-15分钟完成。

### 方式2：快速验证测试

如果容器已经在运行，快速检查系统状态：

```bash
cd tests/integration
./single-machine-test.sh quick
```

### 方式3：运行特定测试场景

```bash
cd tests/integration
./single-machine-test.sh scenario <场景名称>
```

可用场景：
- `basic-functionality` - 基础功能测试
- `network-topology` - 网络拓扑测试
- `fault-recovery` - 故障恢复测试
- `dns-synchronization` - DNS同步测试
- `end-to-end` - 端到端完整流程测试

示例：
```bash
./single-machine-test.sh scenario dns-synchronization
```

### 方式4：使用增强测试场景脚本

直接运行增强的测试场景：

```bash
cd tests/integration/scripts
./test-scenarios-enhanced.sh --scenario basic-functionality
```

## 测试场景详解

### 1. 基础功能测试 (basic-functionality)

验证基本的节点启动和通信功能：

- ✅ 创建Docker隔离网络
- ✅ 启动所有节点（primary, daemons, clients）
- ✅ 验证容器健康状态
- ✅ 测试基础网络连接性
- ✅ 检查p2p-ddns进程运行
- ✅ 验证ticket生成和传播
- ✅ 检查日志错误

### 2. 网络拓扑测试 (network-topology)

验证多子网环境中的通信：

- ✅ 同子网节点通信测试（subnet-a）
- ✅ 跨子网节点通信测试（subnet-a ↔ subnet-b）
- ✅ 网络发现机制验证
- ✅ Gossip协议传播验证
- ✅ 网络路由验证

### 3. 故障恢复测试 (fault-recovery)

验证系统的弹性和恢复能力：

- ✅ 节点隔离测试（模拟网络分区）
- ✅ 节点停止和重启测试
- ✅ 故障期间网络持续运行验证
- ✅ 节点恢复后的重新同步

### 4. DNS同步测试 (dns-synchronization)

验证DNS记录的同步机制：

- ✅ 主节点DNS记录验证
- ✅ 守护节点DNS记录验证
- ✅ DNS记录一致性检查
- ✅ 心跳机制验证
- ✅ DNS表输出验证

### 5. 端到端测试 (end-to-end)

完整的系统工作流程验证：

1. 🧹 清理环境
2. 🌐 创建隔离网络
3. 🔨 构建Docker镜像
4. 🚀 启动主节点
5. 🎫 提取网络ticket
6. 👥 启动守护节点
7. ⏳ 等待网络收敛
8. 📡 启动客户端节点
9. 🔄 验证完整网络同步
10. 💥 测试系统弹性
11. 🩹 验证节点恢复
12. ✅ 最终系统验证

## 容器架构

```
┌─────────────────────────────────────────────────────────┐
│                   Docker Host                       │
├─────────────────────────────────────────────────────────┤
│  Subnet A (10.0.1.0/24)                          │
│  ├── primary-node    (10.0.1.10) [主节点]         │
│  ├── daemon-a1      (10.0.1.11) [守护进程]       │
│  ├── daemon-a2      (10.0.1.12) [守护进程]       │
│  └── client-a1      (10.0.1.13) [客户端]         │
├─────────────────────────────────────────────────────────┤
│  Subnet B (10.0.2.0/24)                          │
│  ├── daemon-b1      (10.0.2.11) [守护进程]       │
│  ├── daemon-b2      (10.0.2.12) [守护进程]       │
│  └── client-b1      (10.0.2.13) [客户端]         │
├─────────────────────────────────────────────────────────┤
│  Public (10.0.0.0/24)                            │
│  ├── primary-node    (10.0.0.10)                  │
│  ├── daemon-b1      (10.0.0.11)                  │
│  └── monitor        (10.0.0.20) [监控节点]         │
└─────────────────────────────────────────────────────────┘
```

## 测试验证项

### 网络连接性测试

测试各容器之间的可达性：

```bash
# 同子网测试
daemon-a1 → primary-node
daemon-a1 → daemon-a2
daemon-b1 → daemon-b2

# 跨子网测试
daemon-a1 → daemon-b1
daemon-a2 → daemon-b2

# 客户端测试
client-a1 → daemon-a1
client-b1 → daemon-b1
```

### DNS记录验证

检查各节点中的DNS记录数量和内容：

```bash
# 查看primary节点的DNS记录
docker-compose logs primary-node | grep "Address.*Name.*Last Seen" -A 20

# 查看daemon节点的DNS记录
docker-compose logs daemon-a1 | grep "Address.*Name.*Last Seen" -A 20
```

### 健康检查

验证容器健康状态：

```bash
# 查看所有容器状态
docker-compose ps

# 查看特定容器健康状态
docker inspect p2p-ddns-test-primary-node --format='{{.State.Health.Status}}'
```

## 调试和故障排除

### 查看容器日志

```bash
# 查看所有容器日志
docker-compose logs -f

# 查看特定容器日志
docker-compose logs -f primary-node
docker-compose logs -f daemon-a1
docker-compose logs -f client-a1

# 查看最近100行日志
docker-compose logs --tail=100 primary-node
```

### 进入容器调试

```bash
# 进入primary节点容器
docker-compose exec primary-node /bin/bash

# 进入daemon节点容器
docker-compose exec daemon-a1 /bin/bash

# 检查进程
ps aux | grep p2p-ddns

# 检查网络连接
ping primary-node
ping daemon-a1
ping daemon-b1

# 查看日志文件
cat /app/logs/primary.log
```

### 网络调试

```bash
# 查看Docker网络
docker network ls | grep -E "(subnet-a|subnet-b|public)"

# 查看网络详细信息
docker network inspect subnet-a

# 测试网络隔离
docker-compose exec daemon-a1 ping -c 3 10.0.1.10  # 同网段
docker-compose exec daemon-a1 ping -c 3 10.0.2.11  # 跨网段
```

### 常见问题

**Q: 容器启动失败？**

A: 检查以下几点：
1. Docker服务是否正在运行：`docker info`
2. 端口是否被占用：`lsof -i :8080-8086`
3. 磁盘空间是否充足：`df -h`

**Q: 网络连接失败？**

A: 检查：
1. 网络是否正确创建：`docker network ls`
2. 容器是否连接到正确的网络：`docker inspect <container>`
3. 防火墙是否阻止了连接

**Q: DNS记录没有同步？**

A: 等待足够时间（最多2-3分钟），然后检查：
1. 主节点是否生成了ticket
2. 守护节点是否收到了ticket
3. 日志中是否有发现和加入网络的消息

**Q: 测试超时？**

A: 调整超时时间：
```bash
# 在脚本中设置更长的超时
TIMEOUT=900 ./single-machine-test.sh full
```

## 清理环境

完全清理测试环境：

```bash
cd tests/integration
./single-machine-test.sh cleanup
```

或手动清理：

```bash
# 停止所有容器
docker-compose down

# 删除网络
./networks/cleanup-networks.sh normal

# 清理网络控制规则
./networks/network-control.sh cleanup

# 删除Docker镜像（可选）
docker rmi p2p-ddns-test-primary:test
docker rmi p2p-ddns-test-daemon:test
docker rmi p2p-ddns-test-client:test
docker rmi p2p-ddns-test-base:test
```

## 测试报告

测试完成后，报告将保存在：

```
tests/integration/reports/single-machine-test-YYYYMMDD-HHMMSS.txt
```

报告包含：
- ✅ 测试日期和持续时间
- ✅ 总体测试结果
- ✅ 各测试场景的详细结果
- ✅ 容器状态
- ✅ 下一步操作建议

## 性能基准

正常情况下的预期结果：

| 测试项目 | 预期值 | 说明 |
|---------|--------|------|
| 容器启动时间 | < 60秒 | 所有7个容器 |
| 网络收敛时间 | < 120秒 | 所有节点发现彼此 |
| DNS记录数 | > 5条 | 每个节点 |
| CPU使用率 | < 50% | 每个容器 |
| 内存使用 | < 200MB | 每个容器 |
| 网络延迟 | < 10ms | 同子网内 |

## 高级用法

### 使用Make命令

```bash
cd tests/integration
make help           # 查看所有可用命令
make test           # 运行快速测试
make test-full      # 运行完整测试
make status         # 查看环境状态
make logs           # 查看容器日志
make clean          # 清理环境
```

### 使用Docker Compose直接管理

```bash
# 启动所有服务
docker-compose up -d

# 启动特定服务
docker-compose up -d primary-node daemon-a1

# 查看服务状态
docker-compose ps

# 查看资源使用
docker-compose top

# 停止所有服务
docker-compose down
```

### 网络隔离操作

```bash
# 隔离整个子网
./networks/network-control.sh isolate-network subnet-a

# 恢复子网连接
./networks/network-control.sh restore-network subnet-a

# 隔离单个容器
./networks/network-control.sh isolate-container primary-node

# 恢复单个容器连接
./networks/network-control.sh restore-container primary-node
```

## 持续集成

可以将这些测试集成到CI/CD流程中：

```yaml
# GitHub Actions 示例
- name: Run Integration Tests
  run: |
    cd tests/integration
    ./single-machine-test.sh full

- name: Upload Test Results
  if: always()
  uses: actions/upload-artifact@v3
  with:
    name: integration-test-results
    path: tests/integration/reports/
```

## 贡献指南

添加新的测试场景时：

1. 在 `scripts/test-scenarios-enhanced.sh` 中添加新的测试函数
2. 遵循现有的命名约定：`run_<scenario_name>()`
3. 添加详细的日志输出
4. 更新本README文档
5. 测试新场景在单机环境中的运行情况

## 支持和反馈

如果遇到问题或需要帮助：

1. 查看本文档的"调试和故障排除"部分
2. 检查容器日志获取详细错误信息
3. 查看生成的测试报告
4. 在项目GitHub仓库提交issue

## 许可证

MIT License - 详见项目根目录的LICENCE文件
